# Number Range

Captures an inclusive numeric interval with `start` and `end`. Both bounds must be finite numbers and `start` must be less than or equal to `end`. Optional metadata can be stored alongside the range.

